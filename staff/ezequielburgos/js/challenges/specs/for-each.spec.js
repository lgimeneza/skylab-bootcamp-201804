'use strict'

// describe('forEach()', function(){
//     it('forEach(input, function(v) { output.push(v) }) should fulfill output with input values [" + input + "]', function(){
//         expect(forEach(input, function(v) { output.push(v) })).toBe(result.toString());
//     })

// })