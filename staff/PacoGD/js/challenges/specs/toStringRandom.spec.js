'use strict';

describe('toStringRandom', function () {

    it('...', function () {
        expect(/* ... */).toBe(/* ... */);
    });

    it('should ... throw an error', function () {
        expect(function () {
            /*  */;
        }).toThrow(Error('input num is not a string'));
    });

});
