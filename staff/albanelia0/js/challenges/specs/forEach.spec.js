'use strict';

var arr = [1,2,3];
describe('forEach',function(){
  it('forEach() - without arguments - it should be undefined',function(){
    expect(function(){
      forEach()
    }).toBeDefined();
  });

});
